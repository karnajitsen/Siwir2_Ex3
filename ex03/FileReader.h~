#pragma once
#include <string>

using namespace std;

namespace lbm {

	class FileReader
	{
		public:
			FileReader();
			void readParameters(string filename);
			
			template<typename Type>
			Type getParameter(string valueName)
			{
				return static_cast<Type>("fixme");
			}
		
		private:
			
	};

}
